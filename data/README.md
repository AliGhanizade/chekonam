# Catalog files

`catalog.json` contains the Persian catalog and `catalog.en.json` contains the English catalog.

Keep IDs stable and synchronized between the files. The application stores crossed-out IDs in browser localStorage, so changing an existing ID removes the user's saved preference for that option.
